name: Authentication browser
on:
  pull_request:
    paths:
      - "src/**"
      - "tests/auth-browser/**"
      - "scripts/test-auth-*.mjs"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/auth-browser.yml"
  push:
    branches: [main]
    paths:
      - "src/**"
      - "tests/auth-browser/**"
      - "scripts/test-auth-*.mjs"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/auth-browser.yml"
permissions:
  contents: read
concurrency:
  group: auth-browser-${{ github.ref }}
  cancel-in-progress: true
jobs:
  browser:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        engine: [chromium, webkit]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps ${{ matrix.engine }}
      - run: npm run test:browser:auth
        env:
          CORE_BROWSER_ENGINE: ${{ matrix.engine }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: auth-${{ matrix.engine }}-evidence
          path: test-results/auth-browser
          retention-days: 14
          if-no-files-found: error
